# MySQL connection — adjust user/password/port to match your install
# XAMPP default: root with no password, port 3306
DATABASE_URL=mysql+pymysql://root:@localhost:3306/ricesystem

# Local file storage — relative to backend/ working directory
STORAGE_DIR=../storage
RAW_SUBDIR=raw
ANNOTATED_SUBDIR=annotated
